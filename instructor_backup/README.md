# Instructor backup files

Before class, add a verified fallback reference:

```text
reference.fasta
```

Also record the COI coordinates if this backup is a complete mitochondrial genome.

This folder prevents an NCBI/network problem from blocking the workshop.
